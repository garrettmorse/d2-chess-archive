[build-system]
requires = ["setuptools >= 77.0.3"]
build-backend = "setuptools.build_meta"

[project]
name = "d2-chess-archive"
version = "0.0.0"
dependencies = [
  "numpy",
  "matplotlib",
  "opencv-python"
]